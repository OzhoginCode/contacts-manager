services:
  client:
    build:
      context: client
      dockerfile: Dockerfile
    ports:
      - "80:80"
    restart: always

  server:
    build:
      context: server
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    restart: always

networks:
  network_1:
    driver: bridge