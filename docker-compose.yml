services:
  client:
    build:
      context: client
      dockerfile: Dockerfile
    ports:
      - "80:80"
    restart: always

  # server:
  #   build:
  #     context: server
  #     dockerfile: Dockerfile
  #   restart: always
  #   network_mode: host
  